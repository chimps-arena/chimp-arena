# Brand assets

Drop the source files here with these exact names — the app references them.

| File | What | Source |
| --- | --- | --- |
| `chimp-logo.png` | The circular CHIMP badge (astronaut chimp, amber ring). 1024×1024, **transparent background preferred** (the version with a black square limits reuse). | founders, 2026-09 |
| `chimp-logo-512.png` | 512×512 down-scale — token / DEX / CoinGecko listing icon. Generate with `scripts/process-art.mjs`. | derived |

Used by: `components/nav-bar.tsx` (site logo), `app/nft/metadata` (fallback), OG/share image, favicon.
